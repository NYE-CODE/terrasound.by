# Nginx configs moved to deploy/nginx/
#
# terrasound.by + /api/  -> deploy/nginx/terrasound.by.conf
# admin.terrasound.by      -> deploy/nginx/admin.terrasound.by.conf
#
# VITE_API_URL=https://terrasound.by  (same domain, no api. subdomain)
