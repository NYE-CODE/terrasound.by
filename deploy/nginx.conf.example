# Пример конфигурации nginx для terrasound.by
# Пути и домены замените на свои.

# API
# server {
#     listen 443 ssl http2;
#     server_name api.terrasound.by;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# Витрина (SPA + prerender)
# server {
#     listen 443 ssl http2;
#     server_name terrasound.by www.terrasound.by;
#     root /var/www/terrasound.by/frontend/dist;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }

# Админка
# server {
#     listen 443 ssl http2;
#     server_name admin.terrasound.by;
#     root /var/www/terrasound.by/admin/dist;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }
